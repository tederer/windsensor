#ifndef windsensor_utils_h
#define windsensor_utils_h

/**
 * Returns the number of characters required to represent number as a string (not including any null terminator).
 **/
int charCountOf(int number);

#endif